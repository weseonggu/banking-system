server:
  port: 9000

spring:
  profiles:
    active: native
    group:
      prod: prod
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-repo
eureka:
  client:
    service-url:
      defaultZone: http://localhost:19090/eureka/